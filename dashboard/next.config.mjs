/** @type {import('next').NextConfig} */
const nextConfig = {
    // Configure dashboard to run on port 3001 to avoid conflicts with API server
    // This can also be set via NEXT_PORT environment variable
};

export default nextConfig;
